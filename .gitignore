# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# uv
# (uv.lock IS committed; the venv is not)

# Local data
# (f1fantasy.db IS committed for Vercel deployment; journals are not)
data/*.db-journal
data/fastf1_cache/

# Env / secrets
.env
*.local
